const { alterarUsuario } = require("./alterarUsuario")
const { excluirUsuario } = require("./excluirUsuario")
const { novoUsuario } = require("./novoUsuario")

module.exports = {
    alterarUsuario,
    excluirUsuario,
    novoUsuario
}